# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                               controllers.Application.index
GET         /index                          controllers.Application.indexRedirect
GET         /football                       controllers.Football.start
GET         /tennis                         controllers.Tennis.start
GET         /cricket                        controllers.Cricket.setup
GET         /randomisers                    controllers.Randomisers.setup
POST        /cricket_scoreboard             controllers.Cricket.startMatch

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                   controllers.Assets.at(path="/public", file)
